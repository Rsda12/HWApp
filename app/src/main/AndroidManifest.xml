<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HWApp"
        tools:targetApi="31">
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.HWApp" />
        <activity
            android:name=".OtherQuestionsActivity"
            android:exported="false"
            android:label="@string/title_activity_other_questions"
            android:theme="@style/Theme.HWApp" />
        <activity
            android:name=".ComputerQuestionsActivity"
            android:exported="false"
            android:label="@string/title_activity_computer_questions"
            android:theme="@style/Theme.HWApp" />
        <activity
            android:name=".EnglishQuestionsActivity"
            android:exported="false"
            android:label="@string/title_activity_english_questions"
            android:theme="@style/Theme.HWApp" />
        <activity
            android:name=".MathQuestionsActivity"
            android:exported="false"
            android:label="@string/title_activity_math_questions"
            android:theme="@style/Theme.HWApp" />
        <activity
            android:name=".MarhQU"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HWApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>